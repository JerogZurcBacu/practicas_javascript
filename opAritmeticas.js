var suma = 7 + 12;
var resta = 15 - 5;
var multiplicacion = 5 * 3;
var division = 20 / 2;
var resto = 15 % 5;

// incrementar valor actual de una variable

var librosComprados = 105;

librosComprados = librosComprados + 1;
librosComprados++;

// reducir valor actual de una variable

librosComprados = librosComprados - 1;
librosComprados--;

// incrementar o reducir en valores diferentes a 1

var a = 23;
a = a + 5;
a += 5;
a -= 8;
// tambien se puede usar con multiplicación y división
a *= 2;
a /= 3;
